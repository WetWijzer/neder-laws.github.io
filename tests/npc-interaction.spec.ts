import { test } from '@playwright/test';

test.describe.skip('Legacy WetWijzer NPC interaction tests', () => {
  test('covered the pre-pivot WetWijzer interface', async () => {
    // The app has pivoted to the WetWijzer legal corpus interface. This file stays
    // as a marker for the legacy test area until those scenarios are either
    // deleted or translated into legal-corpus workflows.
  });
});
